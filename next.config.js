/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['rickandmortyapi.com'], // Add the domain(s) of your image sources here
      },
}

module.exports = nextConfig
